<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">Confirm Delete</MudText>
    </TitleContent>
    <DialogContent>
        <MudText Typo="Typo.body1">
            If you click Yes, you will delete all data about @Model.Name.
            This action cannot be undone.
            <br /><br />
            NOTE: @Model.Name will not be harmed.
        </MudText>
    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Default"
                   OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Error"
                   OnClick="Confirm">Yes</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter]
    MudDialogInstance MudDialog { get; set; } = null!;

    [Parameter]
    public FelineEditModel Model { get; set; } = null!;


    void Cancel()
    {
        MudDialog.Cancel();
    }

    void Confirm()
    {
        MudDialog.Close(DialogResult.Ok(true));
    }
}