<AppConfirmModal>
    If you click Yes, you will delete all data about the Fosterer
    "@Name". Cats linked to @Name will not be deleted, however the
    existing Fosterer value will be cleared.
    <em>This action cannot be undone.</em>
</AppConfirmModal>

@code {
    [Parameter] public string Name { get; set; } = string.Empty;
}